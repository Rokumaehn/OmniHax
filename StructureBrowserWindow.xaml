<Window x:Class="OmniHax.StructureBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OmniHax"
        mc:Ignorable="d"
        Title="Structure Browser" Height="560" Width="780"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Address:" VerticalAlignment="Center" Margin="0,0,6,0" />
            <TextBox x:Name="AddressBox" Width="150" VerticalAlignment="Center" KeyDown="AddressBox_KeyDown" />
            <Button Content="Go" Padding="12,4" Margin="6,0,0,0" VerticalAlignment="Center" Click="GoButton_Click" />

            <TextBlock Text="Type:" VerticalAlignment="Center" Margin="16,0,6,0" />
            <ComboBox x:Name="TypeCombo" Width="200" VerticalAlignment="Center" SelectionChanged="TypeCombo_SelectionChanged" />

            <Button Content="Refresh" Padding="12,4" Margin="16,0,0,0" VerticalAlignment="Center" Click="RefreshButton_Click" />

            <TextBlock Text="Rows:" VerticalAlignment="Center" Margin="16,0,6,0" />
            <TextBox x:Name="RowsBox" Width="60" VerticalAlignment="Center" TextAlignment="Right" KeyDown="RowsBox_KeyDown" />
        </WrapPanel>

        <DataGrid x:Name="StructureGrid" Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  SelectionMode="Single"
                  RowHeaderWidth="0"
                  HorizontalScrollBarVisibility="Auto"
                  FontFamily="Consolas">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Address" Width="150" Binding="{Binding Address}" />
                <DataGridTextColumn Header="Hex" Width="220" Binding="{Binding Hex}" />
                <DataGridTextColumn Header="Value" Width="*" Binding="{Binding Value}" />
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="2" Margin="0,8,0,0">
            <Button DockPanel.Dock="Right" Content="Close" Width="90" Padding="0,4" Click="CloseButton_Click" />
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" TextWrapping="Wrap" />
        </DockPanel>
    </Grid>
</Window>
